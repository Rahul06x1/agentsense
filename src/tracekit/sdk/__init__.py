"""Python capture SDK — fast-follow (Weeks 4-6). Placeholder in v0.

Will wrap agent steps for reasoning-level capture on OpenTelemetry GenAI
semantic conventions, calling the same tracekit.redaction module as the proxy so
there is one redaction code path to audit.
"""
